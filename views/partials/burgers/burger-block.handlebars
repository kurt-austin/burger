<li>
	{{burger_name}}

	<button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{!-- {{#if devoured}}SLEEP TIME!{{else}}Devour!{{/if}} --}}
	    {{#if devoured}}Devour!{{/if}}
	</button>

	{{!-- <button class="delete-cat" data-id="{{this.id}}">DELETE!</button> --}}
</li>
